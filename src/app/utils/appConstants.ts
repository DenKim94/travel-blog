// Unterstützte Sprachen
export type SupportedLanguageType = "GER" | "ENG" | "RUS";
export const supportedLanguages: SupportedLanguageType[] = ['GER', 'ENG', 'RUS']; // Erster Eintrag ist die Standardsprache (siehe GlobalStateContext.tsx)

