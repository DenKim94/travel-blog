import * as helperFunctions from "@utils/helperFunctions"

export default async function SearchResultsBoard() {
    await helperFunctions.mockDelay(3000);

    return (
        <div className="search-results-board">
            <p>PLATZHALTER</p>
        </div>
    );
}